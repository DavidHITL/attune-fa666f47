// Export the useConnectionManagement function so it can be imported elsewhere
export { useConnectionActions as useConnectionManagement } from './useConnectionActions';

// The rest of the file can be kept for reference, but we'll move functionality to other files
// to avoid conflict and maintain proper code organization
